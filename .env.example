# Cache settings
RSS_CACHE_TTL=900000
RSS_MAX_ITEMS_PER_FEED=100

# HTTP settings
RSS_REQUEST_TIMEOUT=30000
RSS_MAX_CONCURRENT_FETCHES=5
RSS_USER_AGENT="MCP-RSS/1.0.0"
RSS_FOLLOW_REDIRECTS=true
RSS_MAX_RESPONSE_SIZE=10485760

# Storage settings
# RSS_STORAGE_PATH=./storage

# Cache management
RSS_CACHE_MAX_SIZE=100
RSS_CACHE_CLEANUP_INTERVAL=300000

# Rate limiting
RSS_RATE_LIMIT_PER_MINUTE=60

# Logging
LOG_LEVEL=info
